# Migration Complete

The old backend migration is complete and the current docs reflect the active stack:

- Java Spring Boot API
- Node.js Socket.IO realtime service
- Python FastAPI sandbox

Use `docs/README.md` as the entry point for the current architecture.
