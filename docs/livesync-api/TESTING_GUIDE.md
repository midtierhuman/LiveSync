# LiveSync API Testing Guide

## Overview

This guide explains how to write and run unit and integration tests for `livesync-api`.

## Running Tests

From `livesync-api` directory:

```bash
./gradlew test
```

On Windows:

```powershell
cd livesync-api
.\gradlew.bat test
```
